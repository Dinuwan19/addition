This is a simple Python script that adds two numbers provided by the user.

## How to Use
1. Run the script in a Python environment.
2. Enter two numbers when prompted.
3. The script will display the sum of the two numbers.

